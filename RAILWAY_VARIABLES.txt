BẮT BUỘC:
APP_USERNAME=cua
APP_PASSWORD=<mật khẩu dài ngẫu nhiên>
AUTO_OPEN=false
HOST=0.0.0.0
MEME_TRADER_DATA_DIR=/data
SOLANA_RPC_URL=<Helius hoặc QuickNode Mainnet RPC>
JUPITER_API_KEY=<Jupiter API key>

KHUYẾN NGHỊ:
SOLANA_RPC_FALLBACK_URLS=https://api.mainnet.solana.com
GECKO_ENABLED=false
FAST_TICKER_MS=1500
FAST_TICKER_MAX_TOKENS=90
SCAN_INTERVAL_MS=10000
MOCK_MODE=false

REALTIME (khuyến nghị mạnh — để giá bám sát on-chain như GMGN, không delay
theo chu kỳ polling):
HELIUS_API_KEY=<lấy trong dashboard Helius, có thể dùng chung key với SOLANA_RPC_URL>
REALTIME_ENABLED=true
# Các biến còn lại (HELIUS_WS_URL, REALTIME_MIN_REFETCH_MS, REALTIME_MAX_SUBS,
# REALTIME_FALLBACK_POLL_MS, REALTIME_RECONNECT_MS) có default hợp lý, chỉ
# cần set nếu muốn tinh chỉnh. Không set HELIUS_API_KEY thì app tự rơi về
# polling như bản cũ, không lỗi.

KHÔNG TẠO:
PORT
RAILWAY_VOLUME_MOUNT_PATH

Railway tự inject hai biến trên.
